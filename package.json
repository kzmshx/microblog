{
  "name": "microblog",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "dev:example-warikan": "turbo dev --scope=example-warikan-*",
    "format": "prettier --write \"**/*.{js,json,jsx,ts,tsx,md,mdx}\"",
    "lint": "turbo lint"
  },
  "prettier": {
    "printWidth": 120
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "prettier": "^3.1.0",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  }
}
